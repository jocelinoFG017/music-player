[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "music-player-cli"
version = "1.0.0"
description = "Player e downloader de musicas para linha de comando."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
music-player = "player:cli"
music-download = "downloader:main"
music-update = "atualizar_ytdlp:main"

[tool.setuptools]
py-modules = ["player", "stats", "downloader", "atualizar_ytdlp"]
